<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Meditation Tracker - Vienna Dashboard</title>
<style>
:root{--bg:#0f172a;--sidebar:#1e293b;--card:#1e293b;--text:#f8fafc;--muted:#94a3b8;--accent:#e11d48;--green:#22c55e;--blue:#3b82f6;--purple:#8b5cf6;--orange:#f97316}
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:system-ui,-apple-system,sans-serif;background:var(--bg);color:var(--text);min-height:100vh;display:flex}

/* Sidebar */
.sidebar{width:260px;background:var(--sidebar);padding:1.5rem;border-right:1px solid #334155;position:fixed;height:100vh;overflow-y:auto}
.logo{display:flex;align-items:center;gap:.75rem;margin-bottom:2rem;padding-bottom:1rem;border-bottom:1px solid #334155}
.logo-icon{font-size:2rem}
.logo-text h1{font-size:1.25rem;font-weight:700}
.logo-text p{font-size:.75rem;color:var(--muted)}
.nav-section{margin-bottom:1.5rem}
.nav-section h3{font-size:.7rem;text-transform:uppercase;letter-spacing:.1em;color:var(--muted);margin-bottom:.75rem;padding-left:.5rem}
.nav-link{display:flex;align-items:center;gap:.75rem;padding:.75rem;border-radius:.5rem;color:var(--text);text-decoration:none;transition:all .2s;margin-bottom:.25rem}
.nav-link:hover{background:rgba(255,255,255,.05)}
.nav-link.active{background:var(--accent);color:white}
.nav-link span{font-size:1.1rem}

/* Main Content */
.main{margin-left:260px;flex:1;padding:2rem;min-height:100vh}
.header{display:flex;justify-content:space-between;align-items:center;margin-bottom:2rem;padding-bottom:1rem;border-bottom:1px solid #334155}
.header h2{font-size:1.5rem}
.streak-badge{background:linear-gradient(135deg,var(--purple),var(--accent));padding:.5rem 1rem;border-radius:2rem;font-weight:600;display:flex;align-items:center;gap:.5rem}
.streak-badge .fire{font-size:1.25rem}

/* Stats */
.stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1rem;margin-bottom:2rem}
.stat{background:var(--card);border-radius:1rem;padding:1.5rem;text-align:center;border:1px solid #334155}
.stat-value{font-size:2.5rem;font-weight:700;background:linear-gradient(135deg,var(--purple),var(--accent));-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.stat-label{font-size:.75rem;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin-top:.25rem}

/* Calendar */
.calendar-section{background:var(--card);border-radius:1rem;padding:1.5rem;border:1px solid #334155;margin-bottom:2rem}
.calendar-section h3{margin-bottom:1rem;display:flex;align-items:center;gap:.5rem}
.calendar-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:.5rem}
.calendar-header{font-size:.7rem;color:var(--muted);text-align:center;padding:.5rem;text-transform:uppercase}
.calendar-day{aspect-ratio:1;display:flex;align-items:center;justify-content:center;border-radius:.5rem;font-size:.875rem;background:rgba(255,255,255,.03);border:1px solid #334155;cursor:pointer;transition:all .2s}
.calendar-day:hover{border-color:#475569}
.calendar-day.completed{background:linear-gradient(135deg,var(--purple),var(--accent));border-color:transparent;color:white;font-weight:600}
.calendar-day.today{border:2px solid var(--green)}
.calendar-day.empty{background:transparent;border:none;cursor:default}
.calendar-day.future{opacity:.4;cursor:default}

/* Benefits Section */
.benefits-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:1.5rem;margin-bottom:2rem}
.benefit-card{background:var(--card);border-radius:1rem;padding:1.5rem;border:1px solid #334155}
.benefit-card h4{display:flex;align-items:center;gap:.5rem;margin-bottom:.75rem;color:var(--text)}
.benefit-card h4 .icon{font-size:1.5rem}
.benefit-card p{font-size:.875rem;color:var(--muted);line-height:1.6}
.benefit-card .source{font-size:.75rem;color:var(--purple);margin-top:.75rem;font-style:italic}

/* Recommendations */
.recommendations{background:var(--card);border-radius:1rem;padding:1.5rem;border:1px solid #334155;margin-bottom:2rem}
.recommendations h3{margin-bottom:1rem;display:flex;align-items:center;gap:.5rem}
.rec-list{display:flex;flex-direction:column;gap:1rem}
.rec-item{display:flex;align-items:flex-start;gap:1rem;padding:1rem;background:rgba(255,255,255,.03);border-radius:.75rem;border:1px solid #334155}
.rec-item:hover{border-color:#475569}
.rec-icon{font-size:2rem;min-width:40px;text-align:center}
.rec-info h4{font-size:1rem;margin-bottom:.25rem}
.rec-info .teacher{font-size:.875rem;color:var(--purple)}
.rec-info .duration{font-size:.75rem;color:var(--muted);margin-top:.25rem}
.rec-info p{font-size:.875rem;color:var(--muted);margin-top:.5rem;line-height:1.5}
.rec-badge{font-size:.65rem;padding:.2rem .5rem;border-radius:.25rem;background:rgba(139,92,246,.2);color:var(--purple);margin-left:.5rem}

/* Shopping List */
.shopping-section{background:var(--card);border-radius:1rem;padding:1.5rem;border:1px solid #334155;margin-bottom:2rem}
.shopping-section h3{margin-bottom:1rem;display:flex;align-items:center;gap:.5rem}
.shopping-list{list-style:none}
.shopping-item{display:flex;align-items:center;gap:.75rem;padding:.75rem;border-bottom:1px solid #334155}
.shopping-item:last-child{border-bottom:none}
.shopping-item input[type="checkbox"]{width:1.25rem;height:1.25rem;accent-color:var(--accent)}
.shopping-item label{font-size:.95rem;cursor:pointer}
.shopping-item.checked label{text-decoration:line-through;color:var(--muted)}
.add-item{display:flex;gap:.5rem;margin-top:1rem;padding-top:1rem;border-top:1px solid #334155}
.add-item input{flex:1;padding:.75rem;border-radius:.5rem;border:1px solid #334155;background:rgba(255,255,255,.05);color:var(--text);font-size:.875rem}
.add-item input::placeholder{color:var(--muted)}
.add-item button{padding:.75rem 1rem;border-radius:.5rem;border:none;background:var(--accent);color:white;cursor:pointer;font-size:.875rem;font-weight:600}
.add-item button:hover{opacity:.9}

/* Mobile */
@media(max-width:768px){
.sidebar{display:none}
.main{margin-left:0}
}
</style>
</head>
<body>

<!-- Sidebar -->
<nav class="sidebar">
<div class="logo">
<span class="logo-icon">🫀</span>
<div class="logo-text">
<h1>Vienna</h1>
<p>Executive Assistant</p>
</div>
</div>

<div class="nav-section">
<h3>Dashboard</h3>
<a href="index.html" class="nav-link"><span>🏠</span> Home</a>
<a href="todo.html" class="nav-link"><span>✅</span> To-Do List</a>
<a href="meditation.html" class="nav-link active"><span>🧘</span> Meditation</a>
</div>

<div class="nav-section">
<h3>Dr. B's Brand</h3>
<a href="style-guide/" class="nav-link"><span>👔</span> Style Guide</a>
<a href="gallery/" class="nav-link"><span>🖼️</span> Image Gallery</a>
</div>

<div class="nav-section">
<h3>Projects</h3>
<a href="surrey-womens-clinic/" class="nav-link"><span>🏥</span> Surrey Clinic</a>
</div>
</nav>

<!-- Main Content -->
<main class="main">
<div class="header">
<h2>🧘 Meditation Tracker</h2>
<div class="streak-badge"><span class="fire">🔥</span> <span id="streakCount">19</span> Day Streak</div>
</div>

<!-- Stats -->
<div class="stats">
<div class="stat">
<div class="stat-value" id="totalDays">19</div>
<div class="stat-label">Days Completed</div>
</div>
<div class="stat">
<div class="stat-value" id="totalMinutes">380</div>
<div class="stat-label">Total Minutes</div>
</div>
<div class="stat">
<div class="stat-value" id="currentStreak">19</div>
<div class="stat-label">Current Streak</div>
</div>
<div class="stat">
<div class="stat-value">20</div>
<div class="stat-label">Daily Goal (min)</div>
</div>
</div>

<!-- Calendar -->
<div class="calendar-section">
<h3>📅 January/February 2026</h3>
<div class="calendar-grid" id="calendar">
<!-- Generated by JS -->
</div>
</div>

<!-- Science-Backed Benefits -->
<h3 style="margin-bottom:1rem">🧠 Science-Backed Benefits of 20-Minute Daily Meditation</h3>
<div class="benefits-grid">
<div class="benefit-card">
<h4><span class="icon">🧠</span> Structural Brain Changes</h4>
<p>8 weeks of meditation increases gray matter in the hippocampus (memory, learning) and reduces gray matter in the amygdala (stress, anxiety). Your brain literally reshapes itself.</p>
<div class="source">— Harvard/MGH, Psychiatry Research: Neuroimaging, 2011</div>
</div>
<div class="benefit-card">
<h4><span class="icon">❤️</span> Cardiovascular Benefits</h4>
<p>20 minutes of meditation lowers blood pressure by 4-5 mmHg systolic. Also reduces cortisol, heart rate variability improves, and inflammatory markers decrease.</p>
<div class="source">— AHA Scientific Statement, Hypertension, 2017</div>
</div>
<div class="benefit-card">
<h4><span class="icon">🎯</span> Enhanced Focus & Attention</h4>
<p>Just 4 days of 20-minute sessions improves attention, working memory, and executive function. After 8 weeks, sustained attention dramatically improves.</p>
<div class="source">— Zeidan et al., Consciousness & Cognition, 2010</div>
</div>
<div class="benefit-card">
<h4><span class="icon">😌</span> Stress & Anxiety Reduction</h4>
<p>Meditation reduces anxiety by 60% in clinical settings. Cortisol levels drop measurably after just one session, with cumulative effects over time.</p>
<div class="source">— Goyal et al., JAMA Internal Medicine, 2014</div>
</div>
<div class="benefit-card">
<h4><span class="icon">💤</span> Improved Sleep Quality</h4>
<p>Mindfulness meditation improves sleep quality and reduces insomnia severity by 50%+. Evening sessions particularly effective for sleep onset.</p>
<div class="source">— Black et al., JAMA Internal Medicine, 2015</div>
</div>
<div class="benefit-card">
<h4><span class="icon">🔬</span> Telomere Protection</h4>
<p>Long-term meditators show 30% higher telomerase activity, suggesting meditation may slow cellular aging. Even short-term practice shows benefits.</p>
<div class="source">— Epel et al., Psychoneuroendocrinology, 2009</div>
</div>
</div>

<!-- Recommendations -->
<div class="recommendations">
<h3>🎧 Recommended 20-Minute Meditations on Insight Timer</h3>
<div class="rec-list">
<div class="rec-item">
<div class="rec-icon">⭐</div>
<div class="rec-info">
<h4>Do Nothing: Awareness Meditates You <span class="rec-badge">YOUR FAVORITE</span></h4>
<div class="teacher">Pat Divilly</div>
<div class="duration">~20 min • Effortless awareness</div>
<p>"Let whatever happens happen. When you notice an intention to control your attention, drop that intention." Perfect for busy minds that try too hard.</p>
</div>
</div>
<div class="rec-item">
<div class="rec-icon">🌊</div>
<div class="rec-info">
<h4>20-Minute Deep Rest (Yoga Nidra)</h4>
<div class="teacher">Ally Boothroyd</div>
<div class="duration">20 min • Body scan, deep relaxation</div>
<p>Yoga Nidra = conscious sleep. Deeply restorative, great for recovery days. One 20-min session = 2-3 hours of sleep quality rest.</p>
</div>
</div>
<div class="rec-item">
<div class="rec-icon">🌬️</div>
<div class="rec-info">
<h4>Breath & Presence</h4>
<div class="teacher">Michael Sealey</div>
<div class="duration">20 min • Breath-focused</div>
<p>Smooth, calming voice. Focuses on breath awareness without being too prescriptive. Good for morning clarity.</p>
</div>
</div>
<div class="rec-item">
<div class="rec-icon">🔥</div>
<div class="rec-info">
<h4>Morning Motivation & Energy</h4>
<div class="teacher">Kenneth Soares</div>
<div class="duration">20 min • Visualization, intention setting</div>
<p>Sets the tone for the day. Visualization-based — see your day going well before it happens. Good pre-case mental prep.</p>
</div>
</div>
<div class="rec-item">
<div class="rec-icon">🧠</div>
<div class="rec-info">
<h4>Focused Attention Practice</h4>
<div class="teacher">Sam Harris (Waking Up collab)</div>
<div class="duration">20 min • Pure attention training</div>
<p>No-frills, direct instructions. Trains the attention like a muscle. Sam's approach is philosophical but practical.</p>
</div>
</div>
<div class="rec-item">
<div class="rec-icon">🌙</div>
<div class="rec-info">
<h4>Evening Wind Down</h4>
<div class="teacher">Sarah Blondin</div>
<div class="duration">20 min • Self-compassion, release</div>
<p>Poetic and gentle. Good for processing a heavy day. Her voice is incredibly soothing — great 9 PM option.</p>
</div>
</div>
</div>
</div>

<!-- Shopping List -->
<div class="shopping-section">
<h3>🛒 Shopping List</h3>
<ul class="shopping-list" id="shoppingList">
<li class="shopping-item">
<input type="checkbox" id="item1">
<label for="item1">Razors</label>
</li>
<li class="shopping-item">
<input type="checkbox" id="item2">
<label for="item2">Aftershave gel</label>
</li>
<li class="shopping-item">
<input type="checkbox" id="item3">
<label for="item3">Distilled water</label>
</li>
</ul>
<div class="add-item">
<input type="text" id="newItem" placeholder="Add new item...">
<button onclick="addItem()">Add</button>
</div>
</div>

</main>

<script>
// Meditation data - started Jan 19, 2026 (19 consecutive days through Feb 6)
const startDate = new Date('2026-01-19');
const today = new Date();
today.setHours(0,0,0,0);

// Generate completed dates (Jan 19 - Feb 6 = 19 days)
const completedDates = [];
for(let i = 0; i < 19; i++) {
    const d = new Date(startDate);
    d.setDate(d.getDate() + i);
    completedDates.push(d.toISOString().split('T')[0]);
}

// Save/load from localStorage
function saveData() {
    localStorage.setItem('meditationDates', JSON.stringify(completedDates));
    localStorage.setItem('shoppingList', document.getElementById('shoppingList').innerHTML);
}

function loadData() {
    const saved = localStorage.getItem('shoppingList');
    if(saved) document.getElementById('shoppingList').innerHTML = saved;
}

// Generate calendar
function generateCalendar() {
    const cal = document.getElementById('calendar');
    cal.innerHTML = '';
    
    // Headers
    ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].forEach(day => {
        cal.innerHTML += `<div class="calendar-header">${day}</div>`;
    });
    
    // Start from Jan 1, 2026
    const calStart = new Date('2026-01-01');
    const calEnd = new Date('2026-02-28');
    
    // Empty cells for days before Jan 1 (Thursday = 4)
    for(let i = 0; i < 4; i++) {
        cal.innerHTML += `<div class="calendar-day empty"></div>`;
    }
    
    // Days
    const current = new Date(calStart);
    while(current <= calEnd) {
        const dateStr = current.toISOString().split('T')[0];
        const dayNum = current.getDate();
        const isCompleted = completedDates.includes(dateStr);
        const isToday = current.getTime() === today.getTime();
        const isFuture = current > today;
        
        let classes = 'calendar-day';
        if(isCompleted) classes += ' completed';
        if(isToday) classes += ' today';
        if(isFuture) classes += ' future';
        
        // Show month indicator on 1st
        const label = dayNum === 1 ? (current.getMonth() === 0 ? 'J1' : 'F1') : dayNum;
        
        cal.innerHTML += `<div class="${classes}" data-date="${dateStr}">${label}</div>`;
        current.setDate(current.getDate() + 1);
    }
    
    updateStats();
}

function updateStats() {
    document.getElementById('totalDays').textContent = completedDates.length;
    document.getElementById('totalMinutes').textContent = completedDates.length * 20;
    document.getElementById('currentStreak').textContent = completedDates.length;
    document.getElementById('streakCount').textContent = completedDates.length;
}

// Shopping list
function addItem() {
    const input = document.getElementById('newItem');
    const text = input.value.trim();
    if(!text) return;
    
    const list = document.getElementById('shoppingList');
    const id = 'item' + Date.now();
    const li = document.createElement('li');
    li.className = 'shopping-item';
    li.innerHTML = `<input type="checkbox" id="${id}"><label for="${id}">${text}</label>`;
    list.appendChild(li);
    input.value = '';
    saveData();
}

// Checkbox handling
document.getElementById('shoppingList').addEventListener('change', function(e) {
    if(e.target.type === 'checkbox') {
        e.target.closest('.shopping-item').classList.toggle('checked', e.target.checked);
        saveData();
    }
});

// Enter key for adding items
document.getElementById('newItem').addEventListener('keypress', function(e) {
    if(e.key === 'Enter') addItem();
});

// Init
generateCalendar();
loadData();
</script>

</body>
</html>
